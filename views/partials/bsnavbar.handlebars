<nav class="navbar navbar-expand-sm navbar-dark navbar-custom">
    <!-- Brand/logo -->
    <a class="navbar-brand" href="/">
        charge<span class="asterisk">.</span>me
    </a>
    <!-- Collaped button -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapse following items... -->
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="nav navbar-nav ml-auto">
            {{#if session.username}}
            <a href="/logout" class="btn btn-outline-light my-2 my-sm-0" role="button">
                <i class="fa fa-sign-out" aria-hidden="true"></i> Logout</a> <!-- add a nice glyph icon --> 
            {{else}}
            <a href="/login" class="btn btn-outline-light my-2 my-sm-0" role="button">
                <i class="fa fa-sign-in" aria-hidden="true"></i> Login</a> <!-- add a nice glyph icon --> 
            {{/if}}
        </ul>
    </div>
</nav>

